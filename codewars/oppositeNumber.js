function opposite(number) {
    return number * -1
  }
opposite(4)

// holy crap that was easier than expected. Turns out I had it right all along, but I needed to enter "return asdfs", not "let asdfs".

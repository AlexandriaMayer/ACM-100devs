console.log("private property? in my program?")

const sw = new Stopwatch(){
    // ok how to count time in JS?
    // start. throw Error() if hit twice
    // stop.  throw Error() if hit twice
    // reset
}
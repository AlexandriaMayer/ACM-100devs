//Create a Netflix TV Show class with a constructor that makes Netflix TV Shows with 4 properties and 3 methods

class NetflixShow {
    constructor(name, epCount, rating, genre){
        this.name = showName 
        this.epCount = numOfEpisodes
        this.rating = userRating
        this.genre = showGenre


}
    playShow(showName){
    console.log(`Now playing ${showName}.`)
}
    length(numOfEpisodes){
        console.log(`${numOfEpisodes} long.`)
    }
    genre(showGenre){
        console.log(`${showGenre}`)
    }
}
let strangerThings = new NetflixShow('Stranger Things', 12, 4.9, 'mystery')
